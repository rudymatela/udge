$ curl -s udge/submit | grep Submit
<h3> Submit your solution </h3>
  <input type="submit" name="submit" value="Submit">
$ curl -sXPOST udge/submit | grep TypeError:  # TODO: FIXME: should be error 400
TypeError: not indexable
$ curl -w "%{http_code}\n" -sXPOST udge/submit --data language=c # TODO: FIXME: should be error 400
200
$ curl -w "%{http_code}\n" -sXPOST udge/submit --data-urlencode "user=user&password=pass" # TODO: FIXME: there should be a result
200
$ user=test-submit-`date "+%Y%m%d-%H%M%S"`
$ curl -w "%{http_code}\n" -sXPOST udge/new-user --data-urlencode "user=$user" --data-urlencode "email=test-user@example.com" --data-urlencode "email-confirmation=test-user@example.com" --data-urlencode "password=the-password" --data-urlencode "password-confirmation=the-password" | grep h2
<h2>User successfully created</h2>
$ curl -w "%{http_code}\n" -sXPOST udge/submit -Fuser=$user -Fpassword=the-password -Fproblem=sum2 -Flanguage=c -Fprogram=@examples/hello.c | grep h2
<h2>Submission successful</h2>
$ find /tmp/submissions/$user -type f | sed -e "s/$user/<user>/;s/[0-9][0-9-]*-[0-9-]*/YYYYMMDD-HHMMSS/g"
/tmp/submissions/<user>/YYYYMMDD-HHMMSS/sum2.c
