#!/bin/bash
#
# usage: ./bin/markdown file.md > file.html
prefix="$(dirname "$(dirname "$0")")"
. "$prefix/bin/utils/core"
. "$bin/utils/html"

html-header "`head -1 "$1"`"
markdown "$@"
html-navbar
html-footer
