#!/bin/bash
#
# usage: udge-update-all-users-html [force]
prefix="$(dirname "$(dirname "$0")")"
. "$prefix/lib/udge/core"
force=$1

for user in `ls-users`
do
	$bin/udge-update-user-html $user $force
done
